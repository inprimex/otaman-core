Agents working inside a git worktree are now correctly identified by the repo they belong to, instead of falling back to a stale environment value — so a worktree session no longer risks acting as the wrong agent.
