`owner-paths` patterns in `platform.yaml` now follow one consistent rule everywhere ownership is resolved: `*` matches within a single path segment (use `**` to cross directories), and a bare directory name — e.g. `src` — owns its whole subtree, anchored at the repo root. Previously the CLI and the bus dispatcher could disagree about who owns a file.
