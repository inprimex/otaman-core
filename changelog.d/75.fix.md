`otaman validate` now accepts the `release:` block in `platform.yaml` (with `release.require_human_review`); it was previously rejected as an unknown key, failing validation on projects that configured release-cut review.
