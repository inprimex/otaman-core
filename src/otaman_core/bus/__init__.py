"""otaman_core.bus — bus-side primitives consumed by the dispatcher.

The dispatcher itself (path-based vs to:-based recipient resolution)
lives in :mod:`otaman_core.bus.dispatch`. Concrete transports live in
otaman-plugin / otaman-bridge.
"""
